
per connettersi:
admin:
sqlplus system/oracle@//localhost:1521/XE
user:
sqlplus ORACLEUSR/ORACLEUSR@//localhost:1521/XE



da java:


public static void saveXmlToOracle(String xmlData) {
        String url = "jdbc:oracle:thin:@//localhost:1521/xe";
        String user = " ORACLEUSR";
        String password = " ORACLEUSR";
        //String sql = "INSERT INTO APP_TABLE (XML) VALUES (XMLTYPE(?))";
        String sql =  "INSERT INTO APP_TABLE (XML) VALUES (XMLTYPE(?))";
        try (Connection conn = DriverManager.getConnection(url, user, password);
             PreparedStatement pstmt = conn.prepareStatement(sql)) {
//            InputStream inputStream = new ByteArrayInputStream(xmlData.getBytes());
//            pstmt.setBinaryStream(1, inputStream, xmlData.length());
        	 pstmt.setString(1, xmlData);
            pstmt.executeUpdate();
        } catch (SQLException e) {
            e.printStackTrace();
        }
    }


per creare una table con id autoincrement:
CREATE TABLE RUBRICA (
  id NUMBER GENERATED BY DEFAULT ON NULL AS IDENTITY,
  nome VARCHAR2(50),
  cognome VARCHAR2(50),
  CONSTRAINT RUBRICA_PK PRIMARY KEY (id)
);


